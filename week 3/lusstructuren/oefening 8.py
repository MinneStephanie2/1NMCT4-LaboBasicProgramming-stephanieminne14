def print_getallen ():
    einde = 100
    som = 0
    optellen = 1
    while optellen <= einde:
        som = som + optellen
        optellen += 1
    print ("de som is {0}".format(som))
print_getallen()
